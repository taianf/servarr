# Radarr — Movie automation
# - Config + DB on host at /data/.state/radarr
# - Reads/writes /data/media/movies
# - Reads /data/downloads
# - HTTP service on port 7878
radarr:
  image:
    repository: lscr.io/linuxserver/radarr
    tag: latest
    pullPolicy: Always
  enabled: true

  env:
    PUID: "1000"
    PGID: "169"
    TZ: "Europe/Lisbon"
    UMASK: "002"

  service:
    type: ClusterIP
    port: 7878

  persistence:
    # Use our pre-created hostPath-backed PVC for /config.
    enabled: true
    existingClaim: servarr-radarr-config

  podSecurityContext:
    fsGroup: 169

  extraVolumes:
    - name: data
      hostPath:
        path: /data
        type: Directory
    - name: tmp
      emptyDir: {}

  extraVolumeMounts:
    - name: data
      mountPath: /data
    - name: tmp
      mountPath: /tmp

  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      memory: 1Gi
